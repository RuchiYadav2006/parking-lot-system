#include "FeeCalculator.h"
#include <stdexcept>

double FeeCalculator::calculateFee(VehicleType type, long durationHours) {
    // TODO:
    // Pick a rate per hour for each vehicle type, e.g.:
    //   BIKE  -> 10 per hour
    //   CAR   -> 20 per hour
    //   TRUCK -> 30 per hour
    // Return rate * durationHours
    //
    // hint: switch on `type` like in VehicleFactory.cpp
    throw std::logic_error("Not implemented");
}
